@using Microsoft.AspNetCore.Http;
@inject IHttpContextAccessor HttpContextAccessor;

<ul class="navbar-nav">
    @{ 
        ViewBag.Data = @HttpContextAccessor.HttpContext.Session.GetString("username");
    }
    @if (ViewBag.Data!=null)
    {
 <li class="nav-item">
            <a id="manage" class="nav-link text-dark" asp-controller="Home" asp-action="Privacy" title="Manage">Hello @ViewBag.Data!</a>
        </li>
        <li class="nav-item">
            @*<form id="logoutForm" class="form-inline" asp-controller="Login"  asp-action="Logout" asp-route-returnUrl="@Url.Action("Login", "Login", new { area = "" })">*@
            <form>
                <button id="logout" type="submit" class="btn btn-dark" asp-controller="Login" asp-action="Logout"><b>Logout</b></button>
            </form>
            @*</form>*@
        </li>
    }
else
{
  <li class="nav-item">
            <a class="btn btn-dark" id="register" asp-controller="Login" asp-action="register"><b>Register</b></a>
            @*@Html.ActionLink("Register","Create","Users")*@
        </li>
        <li class="nav-item">
            <a class="btn btn-dark" id="login" asp-controller="Login" asp-action="login"><b>Login</b></a>
        </li>
}